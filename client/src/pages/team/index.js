import React, { Fragment } from "react";
import TeamHelper from "../../components/team/index.js";

const TeamPage = props => {
    return (
        <Fragment>
            <TeamHelper props={props} />
        </Fragment>
    );
}
export default TeamPage;